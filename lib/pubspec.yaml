name: flutter_planets_crud
version: 1.0.0+1
description: Gerenciador de planetas utilizando Flutter e SQLite
publish_to: 'none'

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.2+1
  path_provider: ^2.0.11
  path: ^1.8.0
  http: ^0.13.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
```
